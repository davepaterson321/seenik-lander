# Guidelines

Project conventions for humans and AI live in the repo root **[`AGENTS.md`](../AGENTS.md)**.

Cursor also supports **`.cursor/rules/`** for scoped rules (globs, always-apply). This folder is kept as a lightweight pointer so older links to `guidelines/Guidelines.md` still resolve.
